# プロジェクトルール (NutriLens)

- **バージョン表示と更新ルール**: 
  - アプリ画面上部（ヘッダー）にバージョン番号を表示すること。
  - **アプリのコード修正（機能拡張、バグ修正、UI変更等）を行った場合は、必ずバージョン番号（例: `v1.0.1` -> `v1.0.2`）を更新すること。**
  - バージョン番号は `app.js` の `App.version` および `index.html` の `#app-version` で管理・表示する。
